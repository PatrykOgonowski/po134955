import javax.swing.*;
public class GUI extends JFrame{

    GUI(String tytul, int x, int y){
        this.setTitle(tytul);
        this.setSize(x,y);
        this.setLocationRelativeTo(null);
    }

}
